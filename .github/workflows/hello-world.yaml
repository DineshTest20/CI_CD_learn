name: hello-world
on: push
jobs:
  helo-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: check out repository
        uses: actions/checkout@v3
      - run: echo "$(cat hello_world.txt)"
       
  
